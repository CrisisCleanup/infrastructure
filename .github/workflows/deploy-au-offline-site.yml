# AUTOMATICALLY GENERATED FILE, DO NOT EDIT MANUALLY.
# Generated by AWS CDK and [cdk-pipelines-github](https://github.com/cdklabs/cdk-pipelines-github)

name: Deploy AU Offline Site
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  build-deploy-au-offline-site-synth:
    name: Synthesize
    permissions:
      contents: read
      id-token: write
    runs-on: ${{inputs.runner || 'ubuntu-latest'}}
    needs: []
    env:
      CI: "true"
      GIGET_AUTH: ${{secrets.GH_CONFIGS_RO_PAT}}
      NX_NON_NATIVE_HASHER: "true"
      NX_BRANCH: ${{github.event.number}}
      NX_RUN_GROUP: ${{github.run_id}}
      NX_CLOUD_ACCESS_TOKEN: ${{secrets.NX_CLOUD_ACCESS_TOKEN}}
      CCU_CONFIGS_DECRYPT: "false"
      MAINTENANCE_SITE_SOURCE: ${{github.workspace}}/.au-offline-site
    steps:
      - name: Mask values
        run: |-
          echo ::add-mask::${{secrets.AWS_PIPELINE_ACCOUNT_ID}}
          echo ::add-mask::${{secrets.AWS_ACCOUNT_ID_AU_OFFLINE_PIPELINE}}
      - name: Checkout
        uses: actions/checkout@v3
        with:
          repository: CrisisCleanup/infrastructure
          ref: main
      - name: Setup PNPM
        uses: pnpm/action-setup@v2.4.0
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: "18"
          cache: pnpm
      - name: Install SOPs
        uses: CrisisCleanup/mozilla-sops-action@main
        with:
          version: 3.8.1
      - name: Install AWS CLI
        uses: unfor19/install-aws-cli-action@v1
        if: runner.arch == 'ARM64' && (inputs.runner && !contains(inputs.runner,
          'ubuntu'))
        with:
          arch: arm64
          version: 2.15.49
      - name: Install Helm
        uses: azure/setup-helm@v3
        with:
          version: 3.14.4
      - name: Authenticate Via OIDC Role
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-region: us-east-1
          role-duration-seconds: 1800
          role-skip-session-tagging: true
          role-to-assume: arn:aws:iam::${{secrets.AWS_PIPELINE_ACCOUNT_ID}}:role/GitHubActionRole
          role-session-name: deploy-au-offline-site
      - name: Pull cdk.context.json
        continue-on-error: true
        env:
          S3_SOURCE: s3://crisiscleanup-pipeline-assets/cdk-assets/au-offline-site/cdk.context.json
          S3_DESTINATION: packages/stacks/maintenance-site/cdk.context.json
        run: aws s3 cp ${{env.S3_SOURCE}} ${{env.S3_DESTINATION}}
      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: master
          repository: CrisisCleanup/au-offline-site
          path: .au-offline-site
      - name: Install
        run: pnpm install
      - name: Build
        run: |-
          pnpm build
          pnpm -F 'stacks.maintenance-site' run synth:silent
          cp -r packages/stacks/maintenance-site/cdk.out ./cdk.out
      - name: Push cdk.context.json
        continue-on-error: true
        env:
          S3_SOURCE: packages/stacks/maintenance-site/cdk.context.json
          S3_DESTINATION: s3://crisiscleanup-pipeline-assets/cdk-assets/au-offline-site/cdk.context.json
        run: aws s3 cp ${{env.S3_SOURCE}} ${{env.S3_DESTINATION}}
      - name: Push cdk.out
        env:
          S3_SOURCE: cdk.out
          S3_DESTINATION: s3://crisiscleanup-pipeline-assets/cdk-assets/au-offline-site/${{github.run_id}}-${{github.run_attempt}}/cdk.out
        run: aws s3 sync ${{env.S3_SOURCE}} ${{env.S3_DESTINATION}}
  publish:
    name: Publish Assets
    permissions:
      contents: read
      id-token: write
    outputs:
      asset-hash1: ${{steps.publish.outputs.asset-hash1}}
      asset-hash2: ${{steps.publish.outputs.asset-hash2}}
      asset-hash3: ${{steps.publish.outputs.asset-hash3}}
      asset-hash4: ${{steps.publish.outputs.asset-hash4}}
      asset-hash5: ${{steps.publish.outputs.asset-hash5}}
    runs-on: ${{inputs.runner || 'ubuntu-latest'}}
    needs:
      - Build-deploy-au-offline-site-synth
    steps:
      - name: Mask values
        run: |-
          echo ::add-mask::${{secrets.AWS_PIPELINE_ACCOUNT_ID}}
          echo ::add-mask::${{secrets.AWS_ACCOUNT_ID_AU_OFFLINE_PIPELINE}}
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: "18"
        if: inputs.runner && !contains(inputs.runner, 'ubuntu')
      - name: Install AWS CLI
        uses: unfor19/install-aws-cli-action@v1
        if: runner.arch == 'ARM64' && (inputs.runner && !contains(inputs.runner,
          'ubuntu'))
        with:
          arch: arm64
          version: 2.15.49
      - name: Authenticate Via OIDC Role
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-region: us-east-1
          role-duration-seconds: 1800
          role-skip-session-tagging: true
          role-to-assume: arn:aws:iam::${{secrets.AWS_PIPELINE_ACCOUNT_ID}}:role/GitHubActionRole
          role-session-name: deploy-au-offline-site
      - name: Pull cdk.out
        env:
          S3_SOURCE: s3://crisiscleanup-pipeline-assets/cdk-assets/au-offline-site/${{github.run_id}}-${{github.run_attempt}}/cdk.out
          S3_DESTINATION: cdk.out
        run: aws s3 sync ${{env.S3_SOURCE}} ${{env.S3_DESTINATION}}
      - name: Install cdk-assets
        run: npm install --no-save cdk-assets
      - name: Publish
        id: publish
        run: >-
          targets="./cdk.out/publish-Assets-FileAsset1-step.sh,./cdk.out/publish-Assets-FileAsset2-step.sh,./cdk.out/publish-Assets-FileAsset3-step.sh,./cdk.out/publish-Assets-FileAsset4-step.sh,./cdk.out/publish-Assets-FileAsset5-step.sh"

          echo -n "$targets" | xargs -r -d',' -t -n1 -P2 /bin/bash
  au-offline-pipeline-au-offline-site-deploy:
    name: Deploy auofflinepipelineauofflinesiteD8BF64B5
    permissions:
      contents: read
      id-token: write
    needs:
      - Build-deploy-au-offline-site-synth
      - publish
    runs-on: ${{inputs.runner || 'ubuntu-latest'}}
    steps:
      - name: Mask values
        run: |-
          echo ::add-mask::${{secrets.AWS_PIPELINE_ACCOUNT_ID}}
          echo ::add-mask::${{secrets.AWS_ACCOUNT_ID_AU_OFFLINE_PIPELINE}}
      - name: Authenticate Via OIDC Role
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-region: ap-southeast-2
          role-duration-seconds: 1800
          role-skip-session-tagging: true
          role-to-assume: arn:aws:iam::${{secrets.AWS_PIPELINE_ACCOUNT_ID}}:role/GitHubActionRole
          role-session-name: deploy-au-offline-site
      - name: Assume CDK Deploy Role
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-region: ap-southeast-2
          role-duration-seconds: 1800
          role-skip-session-tagging: true
          aws-access-key-id: ${{ env.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ env.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ env.AWS_SESSION_TOKEN }}
          role-to-assume: arn:aws:iam::${{secrets.AWS_ACCOUNT_ID_AU_OFFLINE_PIPELINE}}:role/cdk-hnb659fds-deploy-role-${{secrets.AWS_ACCOUNT_ID_AU_OFFLINE_PIPELINE}}-ap-southeast-2
          role-external-id: Pipeline
      - id: Deploy
        uses: aws-actions/aws-cloudformation-github-deploy@v1.2.0
        with:
          name: au-offline-site
          template: https://cdk-hnb659fds-assets-${{secrets.AWS_ACCOUNT_ID_AU_OFFLINE_PIPELINE}}-ap-southeast-2.s3.ap-southeast-2.amazonaws.com/${{
            needs.publish.outputs.asset-hash1 }}.json
          no-fail-on-empty-changeset: "1"
          role-arn: arn:aws:iam::${{secrets.AWS_ACCOUNT_ID_AU_OFFLINE_PIPELINE}}:role/cdk-hnb659fds-cfn-exec-role-${{secrets.AWS_ACCOUNT_ID_AU_OFFLINE_PIPELINE}}-ap-southeast-2
concurrency:
  group: deploy-offline-au
  cancel-in-progress: false
